import React, { useState, useEffect } from 'react';

function ServerInfo() {
  const [servers, setServers] = useState([]);

  useEffect(() => {

  }, []);

  return (
    <div>
      <h2>Informace o serverech</h2>
      {}
    </div>
  );
}

export default ServerInfo;
